import { View, Text } from '@tarojs/components'

/**
 * F1: Initialization Config Page
 * - Elder basic info (name, age, health conditions)
 * - Preferences (topics, schedule, communication style)
 * - Family relationship to elder
 */
export default function InitPage() {
  return (
    <View className="page">
      <Text>初始化配置</Text>
    </View>
  )
}
