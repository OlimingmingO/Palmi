"""Hermes Agent fork — cron scheduler.

Reused from Hermes Agent (77KB scheduler + 41KB jobs):
- scheduler.py: Production-grade cron with file lock + 60s tick
- jobs.py: Job registry with per-user task support
"""
